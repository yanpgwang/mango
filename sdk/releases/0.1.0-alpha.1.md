# First mango-sdk alpha

## npm: published

- Package: `mango-sdk@0.1.0-alpha.1`
- Publisher: `yanpgwang`
- Published at: `2026-08-31T06:35:19.903Z`
- Source commit: `358905c07b256d957e70ab1bbafa31168566685e`
- Package page: https://www.npmjs.com/package/mango-sdk/v/0.1.0-alpha.1
- Tarball: https://registry.npmjs.org/mango-sdk/-/mango-sdk-0.1.0-alpha.1.tgz
- SHA-256: `c3a65690bd72418193f5253290bc1e570743e7f9ff86e6fc58278ae2f97f92fe`
- SHA-512 integrity: `sha512-RMUX4VXK+XsXZ5X6Zpz7gExkgQRja4mbZuBCObmOUjJwPfpyM4yk9nU9yhuAkbJxnp89nQr+28s87yio5n/lJQ==`

Registry metadata was checked against the exact tarball inspected before
publication, including its owner and integrity digest. An exact-version install
from the official npm registry used a fresh directory and a fresh npm cache.
Public imports, all 98 operation exports, and a mocked authenticated request
passed the package smoke test. The installed TypeScript declarations passed the
positive and negative typing examples with strict compiler settings.

Install this alpha explicitly:

```sh
npm install mango-sdk@0.1.0-alpha.1
```

This is a functional alpha, not a stable release or an expansion of Mango's
server capabilities. It uses the `alpha` channel. No package contents were
replaced after publication.

The initial registry response also assigned `latest` to this version, despite
the explicit `--tag alpha` upload. An authenticated `npm dist-tag rm mango-sdk
latest` request returned HTTP 400, and a subsequent GET confirmed both tags
still point to `0.1.0-alpha.1`. This label does not establish stable support.
Public documentation uses the exact alpha version instead. The published
tarball is immutable; its original README predates this registry observation.

## PyPI: published

- Package: `mango-sdk==0.1.0a1`
- Maintainer: `YanpengWang`
- Wheel uploaded at: `2026-08-31T06:46:26.521256Z`
- Source distribution uploaded at: `2026-08-31T06:46:27.924801Z`
- Source commit: `358905c07b256d957e70ab1bbafa31168566685e`
- Package page: https://pypi.org/project/mango-sdk/0.1.0a1/
- Registry metadata: https://pypi.org/pypi/mango-sdk/0.1.0a1/json

The inspected wheel and source distribution were uploaded without rebuilding.
PyPI's recorded SHA-256 digests match both original artifacts. Before upload,
both passed separate fresh installations, sync/async mocked requests, the
`py.typed` check, and strict Twine metadata validation.

After publication, both distributions were installed again from the official
PyPI index into separate fresh Python 3.11 virtual environments outside this
checkout, with caching disabled. The source installation forced a build with
`--no-binary mango-sdk`. Both installed-package smoke tests passed: sync/async
mocked authenticated requests, public imports, all 98 operation exports,
version/User-Agent consistency, and the typing marker. No hosted runtime or
live model was called.

Published artifacts:

| Artifact | SHA-256 |
| --- | --- |
| `mango_sdk-0.1.0a1-py3-none-any.whl` | `9fdbc8f287b2561ab430d20e314b8b93b2fd22b406de57094da25f3192ddcff0` |
| `mango_sdk-0.1.0a1.tar.gz` | `d5b2bf2337484b6b46e8ab0b4806e39c7f1bf77f97739d96eebade58bd9b44ae` |

Install this alpha explicitly in a Python 3.11+ virtual environment:

```sh
python -m pip install 'mango-sdk==0.1.0a1'
```

Python imports remain `from mango_sdk import Mango, AsyncMango`. This is a
pre-release, not a stable API commitment. Upload authentication was confined to
the publishing process; no token was added to source, package artifacts, or a
credential file. Post-upload verification used only public registry downloads.

## Other verification

- SDK generated-source checks: all three languages, 98 OpenAPI operations.
- Python: 121 tests, mypy and Ruff passed.
- TypeScript: 20 tests, generated-source drift and declaration checks passed.
- Go SDK: race tests and vet passed; no independent Go release was tagged.
- All three SDKs passed Mango HTTP conformance with test-only storage and model
  implementations. This is not live-provider or service-recovery evidence.
- Documentation typecheck and production build passed before upload.
